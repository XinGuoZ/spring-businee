spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: xig
    password: 123
    url: jdbc:mysql://localhost:31260/business?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&serverTimezone=UTC&useSSL=false&useJDBCCompliantTimezoneShift=true&allowPublicKeyRetrieval=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai
  application:
    name: business
  redis:
    database: 6
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 0

mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志
server:
  port: 9081
logging:
  level:
    com.aixiao.sms: INFO
    root: INFO
    com.springms: INFO
  file:
    max-history: 1000
    max-size: 7MB
    name: /home/xig/server/logs/${spring.application.name}.log